(function(){ function initHero(){ var hero = document.querySelector('.news-hero'); if(!hero) return; // Only on news detail pages: featured image frame exists var img = document.querySelector('.featured-media__frame img'); if(!img) return; var src = img.getAttribute('data-src') || img.getAttribute('src'); if(!src) return; hero.style.setProperty('--hero-bg', 'url("' + src.replace(/"/g,'&quot;') + '")'); hero.classList.add('has-bg'); } if(document.readyState === 'loading'){ document.addEventListener('DOMContentLoaded', initHero); }else{ initHero(); } })();